/* =======================================
 * Generated by Move-To-Yul compiler v0.0
 * ======================================= */

/// @use-src 2:"tests/Resources.move"

object "A2_M" {
    code {
        codecopy(0, dataoffset("A2_M_deployed"), datasize("A2_M_deployed"))
        return(0, datasize("A2_M_deployed"))
    }
    object "A2_M_deployed" {
        code {
            mstore(0, memoryguard(160))

            // Dummy calls to reference callables for Yul optimizer
            A2_M_increment_a(sload(100))
            A2_M_publish(sload(100), sload(101))
            let $dummy0 := A2_M_test(sload(100))
            sstore(200, $dummy0)

            function A2_M_increment_a(addr) {
                let r, $t2, $t3, $t4, $t5, $t6, $t7
                // $t2 := borrow_global<M::S>($t0)
                /// @src 2:409:426
                {
                    let $base_offset := $MakeTypeStorageBase(0, 0x698265eb, addr)
                    if not($StorageLoadU8($base_offset)) {
                      $AbortBuiltin()
                    }
                    $t2 := $MakePtr(true, add($base_offset, 32))
                }
                // $t3 := borrow_field<M::S>.a($t2)
                /// @src 2:451:454
                $t3 := $IndexPtr($LoadU256($t2), 0)
                // $t4 := read_ref($t3)
                /// @src 2:451:454
                $t4 := $LoadU64($t3)
                // $t5 := 1
                /// @src 2:457:458
                $t5 := 1
                // $t6 := +($t4, $t5)
                /// @src 2:455:456
                $t6 := $AddU64($t4, $t5)
                // $t7 := borrow_field<M::S>.a($t2)
                /// @src 2:445:448
                $t7 := $IndexPtr($LoadU256($t2), 0)
                // write_ref($t7, $t6)
                /// @src 2:445:458
                $StoreU64($t7, $t6)
                // return ()
                /// @src 2:445:458
            }

            function A2_M_publish(sg, a) {
                let s, $t3, $t4, $t5
                // $t3 := true
                /// @src 2:212:216
                $t3 := true
                // $t4 := pack M::S2($t3)
                /// @src 2:206:217
                {
                    let $mem := $Malloc(1)
                    $MemoryStoreU8(add($mem, 0), $t3)
                    $t4 := $mem
                }
                // $t5 := pack M::S($t1, $t4)
                /// @src 2:198:218
                {
                    let $mem := $Malloc(40)
                    $MemoryStoreU64(add($mem, 0), a)
                    $MemoryStoreU256(add($mem, 8), $t4)
                    $t5 := $mem
                }
                // move_to<M::S>($t5, $t0)
                /// @src 2:225:232
                {
                    let $base_offset := $MakeTypeStorageBase(0, 0x698265eb, sg)
                    if $StorageLoadU8($base_offset) {
                      $AbortBuiltin()
                    }
                    $StorageStoreU8($base_offset, true)
                    {
                        let $dst := add($base_offset, 32)
                        let $src := $t5
                        $StoreU64(add($dst, 0), $MemoryLoadU64(add($src, 0)))
                        {
                            let $linked_dst := $NewLinkedStorageBase(0x5f8466cf)
                            let $linked_src := $MemoryLoadU256(add($src, 8))
                            $StoreU8(add($linked_dst, 0), $MemoryLoadU8(add($linked_src, 0)))
                            $StorageStoreU256(add($dst, 8), $linked_dst)
                        }
                        {
                            let $field_ptr := $LoadU256(add($src, 8))
                            $Free($field_ptr, 1)
                        }
                        $Free($src, 40)
                    }
                }
                // return ()
                /// @src 2:225:242
            }

            function A2_M_test(addr) -> $result {
                let $t1
                // $t1 := exists<M::S>($t0)
                /// @src 2:307:313
                {
                    $t1 := $StorageLoadU8($MakeTypeStorageBase(0, 0x698265eb, addr))
                }
                // return $t1
                /// @src 2:307:322
                $result := $t1
            }

            function $Abort(code) {
                revert(0, 0) // TODO: convention to store code
            }
            function $AbortBuiltin() {
                $Abort(sub(0, 1))
            }
            function $Malloc(size) -> offs {
                offs := mload(0)
                mstore(0, add(offs, size))
            }
            function $Free(offs, size) {
            }
            function $MakePtr(is_storage, offs) -> ptr {
              ptr := or(is_storage, shl(offs, 1))
            }
            function $IsStoragePtr(ptr) -> b {
              b := and(ptr, 0x1)
            }
            function $OffsetPtr(ptr) -> offs {
              offs := shr(ptr, 1)
            }
            function $ToWordOffs(offs) -> word_offs, bit_offs {
              word_offs := shr(offs, 5)
              bit_offs := shl(and(offs, 0x1F), 3)
            }
            function $StorageKey(type, word) -> key {
              mstore(32, word)
              mstore(64, type)
              key := keccak256(32, 33)
            }
            function $MakeTypeStorageBase(category, type_hash, instance) -> offs {
              offs := or(shl(type_hash, 224), shl(instance, 64))
            }
            function $NewLinkedStorageBase(type_hash) -> offs {
              let handle := mload(128)
              mstore(128, add(handle, 1))
              offs := $MakeTypeStorageBase(1, type_hash, handle)
            }
            function $IndexPtr(ptr, offs) -> new_ptr {
              new_ptr := $MakePtr($IsStoragePtr(ptr), add($OffsetPtr(ptr), offs))
            }
            function $MemoryLoadU8(offs) -> val {
              val := and(mload(offs), 0xff)
            }
            function $StorageLoadU8(offs) -> val {
              let word_offs, bit_offs := $ToWordOffs(offs)
              let key := $StorageKey(0, word_offs)
              val := and(shr(sload(key), bit_offs), 0xff)
            }
            function $StoreU8(ptr, val) {
              let offs := $OffsetPtr(ptr)
              switch $IsStoragePtr(ptr)
              case 0 {
                $MemoryStoreU8(offs, val)
              }
              default {
                $StorageStoreU8(offs, val)
              }
            }
            function $MemoryStoreU8(offs, val) {
              mstore8(offs, val)
            }
            function $StorageStoreU8(offs, val) {
              let word_offs, bit_offs := $ToWordOffs(offs)
              let key := $StorageKey(0, word_offs)
              let word := sload(key)
              word := or(and(word, not(shl(0xff, bit_offs))), shl(val, bit_offs))
              mstore(key, word)
            }
            function $LoadU64(ptr) -> val {
              let offs := $OffsetPtr(ptr)
              switch $IsStoragePtr(ptr)
              case 0 {
                val := $MemoryLoadU64(offs)
              }
              default {
                val := $StorageLoadU64(offs)
              }
            }
            function $MemoryLoadU64(offs) -> val {
              val := and(mload(offs), 0xffffffffffffffff)
            }
            function $StorageLoadU64(offs) -> val {
              let word_offs, bit_offs := $ToWordOffs(offs)
              let key := $StorageKey(0, word_offs)
              val := and(shr(sload(key), bit_offs), 0xffffffffffffffff)
              let used_bits := sub(256, bit_offs)
              if lt(used_bits, 64) {
                let overflow_bits := sub(64, used_bits)
                let mask := sub(shl(1, overflow_bits), 1)
                key := $StorageKey(0, add(word_offs, 1))
                val := or(val, shl(and(sload(key), mask), used_bits))
              }
            }
            function $StoreU64(ptr, val) {
              let offs := $OffsetPtr(ptr)
              switch $IsStoragePtr(ptr)
              case 0 {
                $MemoryStoreU64(offs, val)
              }
              default {
                $StorageStoreU64(offs, val)
              }
            }
            function $MemoryStoreU64(offs, val) {
              mstore(offs, or(and(mload(offs), not(0xffffffffffffffff)), val))
            }
            function $StorageStoreU64(offs, val) {
              let word_offs, bit_offs := $ToWordOffs(offs)
              let key := $StorageKey(0, word_offs)
              let word := sload(key)
              word := or(and(word, not(shl(0xffffffffffffffff, bit_offs))), shl(val, bit_offs))
              mstore(key, word)
              let used_bits := sub(256, bit_offs)
              if lt(used_bits, 64) {
                let overflow_bits := sub(64, used_bits)
                let mask := sub(shl(1, overflow_bits), 1)
                key := $StorageKey(0, add(word_offs, 1))
                sstore(key, or(and(sload(key), not(mask)), shr(val, used_bits)))
              }
            }
            function $LoadU256(ptr) -> val {
              let offs := $OffsetPtr(ptr)
              switch $IsStoragePtr(ptr)
              case 0 {
                val := $MemoryLoadU256(offs)
              }
              default {
                val := $StorageLoadU256(offs)
              }
            }
            function $MemoryLoadU256(offs) -> val {
              val := mload(offs)
            }
            function $StorageLoadU256(offs) -> val {
              let word_offs, bit_offs := $ToWordOffs(offs)
              let key := $StorageKey(0, word_offs)
              val := shr(sload(key), bit_offs)
              let used_bits := sub(256, bit_offs)
              if lt(used_bits, 256) {
                let overflow_bits := sub(256, used_bits)
                let mask := sub(shl(1, overflow_bits), 1)
                key := $StorageKey(0, add(word_offs, 1))
                val := or(val, shl(and(sload(key), mask), used_bits))
              }
            }
            function $MemoryStoreU256(offs, val) {
              mstore(offs, val)
            }
            function $StorageStoreU256(offs, val) {
              let word_offs, bit_offs := $ToWordOffs(offs)
              let key := $StorageKey(0, word_offs)
              let word := sload(key)
              word := or(and(word, not(shl(0xffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff, bit_offs))), shl(val, bit_offs))
              mstore(key, word)
              let used_bits := sub(256, bit_offs)
              if lt(used_bits, 256) {
                let overflow_bits := sub(256, used_bits)
                let mask := sub(shl(1, overflow_bits), 1)
                key := $StorageKey(0, add(word_offs, 1))
                sstore(key, or(and(sload(key), not(mask)), shr(val, used_bits)))
              }
            }
            function $AddU64(x, y) -> r {
                if lt(sub(0xffffffffffffffff, x), y) { $AbortBuiltin() }
                r := add(x, y)
            }
        }
    }
}


!! Optimized Yul

Pretty printed source:
/// @use-src 2:"tests/Resources.move"
object "A2_M" {
    code {
        {
            let _1 := datasize("A2_M_deployed")
            codecopy(0, dataoffset("A2_M_deployed"), _1)
            return(0, _1)
        }
    }
    object "A2_M_deployed" {
        code {
            {
                let _1 := 0
                mstore(_1, memoryguard(0xa0))
                let _2 := shl(sload(100), 64)
                if not($StorageLoadU8(_2)) { revert(_1, _1) }
                let _3 := 1
                let $t2 := or(true, shl(add(_2, 32), _3))
                let _4 := $LoadU256($t2)
                let _5 := shl(shr(_4, _3), _3)
                let _6 := and(_4, _3)
                let val := _1
                let offs := shr(or(_6, _5), _3)
                switch or(_6, and(_5, _3))
                case 0 {
                    val := and(mload(offs), 0xffffffffffffffff)
                }
                default {
                    let val_1 := _1
                    let word_offs, bit_offs := $ToWordOffs(offs)
                    mstore(32, word_offs)
                    mstore(64, _1)
                    val_1 := and(shr(sload(keccak256(32, 33)), bit_offs), 0xffffffffffffffff)
                    let used_bits := sub(256, bit_offs)
                    if lt(used_bits, 64)
                    {
                        mstore(32, add(word_offs, _3))
                        mstore(64, _1)
                        val_1 := or(val_1, shl(and(sload(keccak256(32, 33)), add(shl(_3, add(bit_offs, not(191))), not(0))), used_bits))
                    }
                    val := val_1
                }
                $StoreU64($IndexPtr($LoadU256($t2)), $AddU64(val))
                A2_M_publish(sload(100), sload(101))
                sstore(200, A2_M_test(sload(100)))
            }
            function A2_M_publish(sg, a)
            {
                let _1 := 0
                let offs := mload(_1)
                mstore(_1, add(offs, 1))
                mstore8(offs, true)
                let offs_1 := mload(_1)
                mstore(_1, add(offs_1, 40))
                mstore(offs_1, or(and(mload(offs_1), not(0xffffffffffffffff)), a))
                let _2 := add(offs_1, 8)
                mstore(_2, offs)
                let _3 := shl(sg, 64)
                if $StorageLoadU8(_3) { revert(_1, _1) }
                let word_offs, bit_offs := $ToWordOffs(_3)
                mstore(32, word_offs)
                mstore(64, _1)
                let key := keccak256(32, 33)
                let word := sload(key)
                mstore(key, or(and(word, not(shl(0xff, bit_offs))), shl(true, bit_offs)))
                $StoreU64(add(_3, 32), and(mload(offs_1), 0xffffffffffffffff))
                let $linked_dst := $NewLinkedStorageBase()
                $StoreU8($linked_dst, $MemoryLoadU8(mload(_2)))
                $StorageStoreU256(add(_3, 40), $linked_dst)
                pop($LoadU256(_2))
            }
            function A2_M_test(addr) -> $result
            {
                $result := $StorageLoadU8(shl(addr, 64))
            }
            function $ToWordOffs(offs) -> word_offs, bit_offs
            {
                word_offs := shr(offs, 5)
                bit_offs := shl(and(offs, 0x1F), 3)
            }
            function $NewLinkedStorageBase() -> offs
            {
                let handle := mload(128)
                mstore(128, add(handle, 1))
                offs := shl(handle, 64)
            }
            function $IndexPtr(ptr) -> new_ptr
            {
                new_ptr := or(and(ptr, 1), shl(shr(ptr, 1), 1))
            }
            function $MemoryLoadU8(offs) -> val
            { val := and(mload(offs), 0xff) }
            function $StorageLoadU8(offs) -> val
            {
                let word_offs, bit_offs := $ToWordOffs(offs)
                mstore(32, word_offs)
                mstore(64, 0)
                val := and(shr(sload(keccak256(32, 33)), bit_offs), 0xff)
            }
            function $StoreU8(ptr, val)
            {
                let offs := shr(ptr, 1)
                switch and(ptr, 1)
                case 0 { mstore8(offs, val) }
                default {
                    let word_offs, bit_offs := $ToWordOffs(offs)
                    mstore(32, word_offs)
                    mstore(64, 0)
                    let key := keccak256(32, 33)
                    let word := sload(key)
                    mstore(key, or(and(word, not(shl(0xff, bit_offs))), shl(val, bit_offs)))
                }
            }
            function $StoreU64(ptr, val)
            {
                let offs := shr(ptr, 1)
                switch and(ptr, 1)
                case 0 {
                    mstore(offs, or(and(mload(offs), not(0xffffffffffffffff)), val))
                }
                default {
                    let word_offs, bit_offs := $ToWordOffs(offs)
                    mstore(32, word_offs)
                    mstore(64, 0)
                    let key := keccak256(32, 33)
                    let word := sload(key)
                    mstore(key, or(word, shl(val, bit_offs)))
                    let used_bits := sub(256, bit_offs)
                    if lt(used_bits, 64)
                    {
                        mstore(32, add(word_offs, 1))
                        mstore(64, 0)
                        let key_1 := keccak256(32, 33)
                        sstore(key_1, or(and(sload(key_1), not(add(shl(1, add(bit_offs, not(191))), not(0)))), shr(val, used_bits)))
                    }
                }
            }
            function $LoadU256(ptr) -> val
            {
                let offs := shr(ptr, 1)
                switch and(ptr, 1)
                case 0 { val := mload(offs) }
                default {
                    let val_1 := 0
                    let word_offs, bit_offs := $ToWordOffs(offs)
                    mstore(32, word_offs)
                    mstore(64, val_1)
                    val_1 := shr(sload(keccak256(32, 33)), bit_offs)
                    let _1 := 256
                    let used_bits := sub(_1, bit_offs)
                    if lt(used_bits, _1)
                    {
                        mstore(32, add(word_offs, 1))
                        mstore(64, 0)
                        val_1 := or(val_1, shl(and(sload(keccak256(32, 33)), add(shl(1, bit_offs), not(0))), used_bits))
                    }
                    val := val_1
                }
            }
            function $StorageStoreU256(offs, val)
            {
                let word_offs, bit_offs := $ToWordOffs(offs)
                mstore(32, word_offs)
                mstore(64, 0)
                let key := keccak256(32, 33)
                let word := sload(key)
                mstore(key, or(word, shl(val, bit_offs)))
                let _1 := 256
                let used_bits := sub(_1, bit_offs)
                if lt(used_bits, _1)
                {
                    mstore(32, add(word_offs, 1))
                    mstore(64, 0)
                    let key_1 := keccak256(32, 33)
                    sstore(key_1, or(and(sload(key_1), not(add(shl(1, bit_offs), not(0)))), shr(val, used_bits)))
                }
            }
            function $AddU64(x) -> r
            {
                if lt(sub(0xffffffffffffffff, x), 1) { revert(0, 0) }
                r := add(x, 1)
            }
        }
    }
}
