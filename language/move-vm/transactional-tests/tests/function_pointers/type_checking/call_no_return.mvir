//# publish
module 0x42.M {
    public sum(a: u64, b: bool) {
    label b0:
        return;
    }
}

//# run
import 0x42.M;

main() {
    let func: |u64, bool| ();
label b0:
    func = get_function_pointer(M.sum);
    call_function_pointer<|u64, bool| ()>(0, true, move(func));
    return;
}
