module 0x1::M {
    use 99::BCS;
    use 0xdE23::BCS;
    use std::bit_vector;
    use 0xAaaF1::event::EventHandle;
    use std::event::EventHandle;
    use std::fixed_point32 as Fp32;
    use std::option::{Self, Option as Opt};
}
